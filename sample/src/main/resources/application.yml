spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jpa_demo?currentSchema=public
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true